[
  {
    "feature_id": "infra-001",
    "tests_written": [
      "all 10 tables exist with correct names",
      "all columns present on each table",
      "not-null constraints on critical columns (slug, orgId, userId, keyHash, action, type)",
      "schema pushed to RDS Postgres successfully"
    ],
    "needs_deeper_qa": [
      "Verify foreign key cascade deletes work in practice (insert parent + child, delete parent, confirm child deleted)",
      "Verify unique index constraints reject duplicates (e.g. duplicate org slug, duplicate membership)",
      "Verify enum columns reject invalid values",
      "Verify jsonb default values are correct ({} vs [])"
    ]
  },
  {
    "feature_id": "infra-002",
    "tests_written": [
      "buildS3Key constructs correct key with orgId/projectId/assets prefix",
      "buildS3Key handles nested paths and strips leading slashes",
      "getUploadPresignedUrl returns presigned URL and key",
      "getUploadPresignedUrl uses correct content type and bucket",
      "getDownloadPresignedUrl returns presigned URL",
      "deleteObject sends delete command",
      "objectExists returns true/false correctly",
      "ALLOWED_CONTENT_TYPES includes images/docs, excludes executables",
      "validateUploadRequest rejects empty filename, bad content types, path traversal",
      "API route POST /api/uploads/presign returns presigned URL",
      "API route rejects missing fields and disallowed content types",
      "API route GET /api/uploads/presign returns download URL"
    ],
    "needs_deeper_qa": [
      "Real S3 upload via presigned URL \u2014 unit tests mock the SDK",
      "CORS headers on S3 bucket \u2014 verify browser can PUT directly",
      "Large file upload (near 50MB limit) \u2014 only validated client-side",
      "Concurrent uploads to same key \u2014 race condition handling",
      "Presigned URL expiry \u2014 verify 15-minute window works correctly"
    ]
  },
  {
    "feature_id": "auth-001",
    "tests_written": [
      "middleware logic: identifies public vs protected paths correctly",
      "middleware logic: redirects unauthenticated users from protected routes to /login",
      "middleware logic: allows unauthenticated users on public paths",
      "middleware logic: redirects authenticated users from /login and /signup to /dashboard",
      "middleware logic: allows authenticated users on protected paths",
      "auth config: exports auth server instance with handler function",
      "auth client: exports authClient with signIn, signOut, useSession",
      "E2E: login page renders with Google OAuth button",
      "E2E: signup page renders with Google OAuth button",
      "E2E: unauthenticated user redirected from /dashboard to /login",
      "E2E: unauthenticated user redirected from /settings to /login",
      "E2E: auth API session endpoint returns null for unauthenticated"
    ],
    "needs_deeper_qa": [
      "Full Google OAuth flow \u2014 requires real Google account login via browser",
      "Session persistence after OAuth callback \u2014 verify cookie is set and session created in DB",
      "Session expiry and refresh \u2014 Better Auth cookie cache set to 5 minutes",
      "Middleware redirect with return URL \u2014 currently redirects to /login without preserving original URL",
      "Concurrent session handling \u2014 multiple tabs/devices",
      "Test-only /api/test/create-session route is NOT accessible in production (NODE_ENV guard)"
    ]
  },
  {
    "feature_id": "auth-002",
    "tests_written": [
      "slugify converts name to lowercase kebab-case",
      "slugify strips special characters",
      "slugify collapses multiple hyphens",
      "slugify trims leading/trailing hyphens",
      "slugify handles unicode by stripping non-ascii",
      "validateOrgName accepts valid names, rejects empty/too-short/too-long",
      "validateCreateOrgRequest rejects missing/non-string name",
      "validateCreateOrgRequest returns trimmed name for valid input",
      "middleware redirects unauthenticated user from /onboarding to /login",
      "middleware allows authenticated user to access /onboarding",
      "E2E: redirects new user without org to /onboarding",
      "E2E: shows org creation form on /onboarding",
      "E2E: creates org and redirects to dashboard",
      "E2E: validates empty org name"
    ],
    "needs_deeper_qa": [
      "Full flow: Google OAuth \u2192 first login \u2192 onboarding \u2192 create org \u2192 dashboard",
      "Slug collision handling \u2014 if org name produces duplicate slug, suffix is appended",
      "Concurrent org creation by same user \u2014 race condition on membership",
      "User with existing org visits /onboarding \u2014 should redirect to /dashboard",
      "Org name with only special characters \u2014 slug would be empty, should return error"
    ]
  },
  {
    "feature_id": "auth-003",
    "tests_written": [
      "generateApiKey produces admin keys with mint_ prefix",
      "generateApiKey produces assistant keys with mint_dsc_ prefix",
      "generateApiKey produces unique keys each call",
      "hashApiKey returns consistent SHA-256 hex digest",
      "verifyApiKey matches correct key/hash, rejects wrong key",
      "maskKeyPrefix shows prefix + first 4 chars + dots",
      "validateCreateApiKeyRequest accepts valid admin/assistant requests",
      "validateCreateApiKeyRequest defaults type to admin",
      "validateCreateApiKeyRequest rejects empty/missing/long names",
      "validateCreateApiKeyRequest rejects invalid type",
      "E2E: page loads with admin and assistant sections",
      "E2E: create admin key via modal, shown once",
      "E2E: create assistant key via modal",
      "E2E: lists keys with masked prefix",
      "E2E: delete/revoke a key"
    ],
    "needs_deeper_qa": [
      "Full create+list+delete flow with real authenticated session",
      "API key validation in API middleware \u2014 keys work for authenticating API requests",
      "Only admin role can create/delete keys \u2014 viewer/editor should get 403",
      "Concurrent key creation by multiple users in same org",
      "Key prefix collision \u2014 extremely unlikely but no unique constraint on keyPrefix",
      "Copy to clipboard on the 'shown once' modal \u2014 requires HTTPS or localhost"
    ]
  },
  {
    "feature_id": "design-001",
    "tests_written": [
      "sidebar has correct main nav items (Home, Editor, Analytics, Settings)",
      "sidebar has correct agent nav items (Agent, Assistant, Workflows, MCP)",
      "nav items have valid href paths",
      "active nav item detection works correctly for all routes",
      "top bar has search, notifications, chat, and profile elements",
      "sidebar collapse toggle works",
      "user menu includes required items with correct order",
      "E2E: dashboard loads with sidebar and top bar",
      "E2E: sidebar shows all nav items",
      "E2E: profile menu opens with user options",
      "E2E: trial banner visible",
      "E2E: greeting displayed"
    ],
    "needs_deeper_qa": [
      "Sidebar collapse animation and state persistence across navigations",
      "Org switcher dropdown \u2014 creating new org from dropdown",
      "Profile menu logout actually clears session and redirects",
      "Theme switcher (System/Light/Dark) \u2014 currently visual only, no state management",
      "Mobile responsiveness \u2014 sidebar should collapse on small screens",
      "Settings layout shares same shell \u2014 verify settings pages show sidebar",
      "Products pages layout shares same shell \u2014 verify products pages show sidebar",
      "Trial banner dismiss persists (currently resets on page reload)"
    ]
  },
  {
    "feature_id": "feature-001",
    "tests_written": [
      "slugifyProject converts name to lowercase kebab-case",
      "slugifyProject strips special chars, collapses hyphens, trims edges",
      "generateSubdomain combines org slug + project slug",
      "generateSubdomain returns just project slug when same as org slug",
      "validateProjectName accepts valid names, rejects empty/short/long",
      "validateCreateProjectRequest validates name, optional repoUrl",
      "validateCreateProjectRequest rejects missing name, invalid URL",
      "validateUpdateProjectRequest validates name, subdomain, repoUrl fields",
      "validateUpdateProjectRequest rejects empty body, invalid subdomain chars",
      "E2E: GET /api/projects returns projects array",
      "E2E: POST /api/projects creates a project with slug and subdomain",
      "E2E: POST /api/projects rejects empty name",
      "E2E: settings general page loads",
      "E2E: new project page loads"
    ],
    "needs_deeper_qa": [
      "Full create project flow with authenticated session — requires Google OAuth login",
      "Settings General page save — verify PUT /api/projects/:id updates name and subdomain in DB",
      "Project slug collision within same org — timestamp suffix appended",
      "DELETE /api/projects/:id — cannot delete last project (400 response)",
      "DELETE /api/projects/:id — only admin role allowed (editor/viewer get 403)",
      "Sidebar project selector — shows list of projects, active project highlighted",
      "New project page — full form submission and redirect to dashboard"
    ]
  },
  {
    "feature_id": "onboard-001",
    "tests_written": [
      "step 1: validates org name (empty, too short, valid)",
      "step 2: GitHub step is skippable",
      "step 3: validates project name (empty, too short, valid)",
      "step 3: generates subdomain from org + project slug",
      "step 3: slugifies project name correctly",
      "wizard has 4 steps in correct order: org, github, project, success",
      "progress indicator has labels for all 4 steps",
      "progress percentage computed correctly per step",
      "back/forward navigation bounds checking",
      "E2E: step 1 shows org creation form with Continue button",
      "E2E: progress indicator shows all 4 step labels",
      "E2E: validates empty org name on step 1",
      "E2E: step 2 shows GitHub connection with skip option",
      "E2E: step 3 shows project creation after skipping GitHub",
      "E2E: validates empty project name on step 3",
      "E2E: full wizard flow org → skip GitHub → project → success",
      "E2E: back button navigates to previous step"
    ],
    "needs_deeper_qa": [
      "Full wizard flow with real Google OAuth session — end-to-end from login to dashboard",
      "GitHub repository URL validation — currently accepts any URL, needs GitHub-specific check",
      "Subdomain preview live update as user types project name",
      "Step 2 Connect repository button with actual GitHub repo URL — verify it's stored on project",
      "Wizard state lost on page refresh — user must restart from step 1",
      "Race condition: rapid clicking Continue creates duplicate orgs",
      "Org/project created but wizard fails at later step — orphaned records"
    ]
  },
  {
    "feature_id": "feature-002",
    "tests_written": [
      "normalizePath converts to lowercase, trims slashes, collapses slashes, converts backslashes",
      "titleFromPath converts kebab-case to title case, uses last segment",
      "validatePagePath accepts valid paths, rejects empty/special chars/path traversal/too long",
      "validatePageTitle accepts valid titles, rejects empty/too long",
      "validateCreatePageRequest validates path+title, normalizes path, includes optional content/description",
      "validateCreatePageRequest rejects missing path/title/null body",
      "validateUpdatePageRequest accepts title/content/isPublished updates, rejects empty/invalid",
      "buildPageTree builds flat and nested trees, sorts folders before files",
      "E2E: editor page loads with Navigation and Files tabs",
      "E2E: shows empty state when no pages exist",
      "E2E: can open create page modal",
      "E2E: can switch between Navigation and Files tabs",
      "E2E: create page modal validates required fields"
    ],
    "needs_deeper_qa": [
      "Full CRUD flow: create page, view in tree, edit content, save, delete — requires authenticated session",
      "Page path uniqueness within project — duplicate path returns 409",
      "Page tree hierarchy — deeply nested paths render correct folder structure",
      "Content editor save — verify PUT updates content in DB and re-renders",
      "Delete page confirmation — verify page removed from tree and content area clears",
      "Concurrent page edits by multiple users — last-write-wins, no conflict resolution",
      "Large MDX content — performance of textarea with very long content",
      "File list view vs Navigation view — both should select and display the same page"
    ]
  },
  {
    "feature_id": "feature-003",
    "tests_written": [
      "extractFrontmatter parses valid frontmatter, handles missing/unclosed/quoted values",
      "serializeFrontmatter serializes with body, skips empty values, quotes special chars",
      "countLines counts single/multiple lines, empty string",
      "generateLineNumbers produces correct array",
      "extractToc extracts headings with levels and sanitized IDs",
      "createAutoSave debounces, cancels, and flushes correctly",
      "mdxSnippets has all required component snippets",
      "insertSnippetAtCursor inserts at beginning/middle/end with correct newlines",
      "E2E: editor renders toolbar with Visual/Markdown mode toggle",
      "E2E: defaults to visual mode",
      "E2E: switches between visual and markdown modes",
      "E2E: markdown editor shows line numbers",
      "E2E: publish button shows popover with site URL",
      "E2E: toolbar has formatting buttons (bold, italic, heading)",
      "E2E: add-new dropdown shows component options"
    ],
    "needs_deeper_qa": [
      "Visual mode WYSIWYG — verify Tiptap renders headings/bold/italic/links correctly",
      "Mode toggle preserves content — switch visual→markdown→visual without data loss",
      "Auto-save with 2s debounce — verify content persists to DB after typing stops",
      "Toolbar formatting in visual mode — bold/italic/heading toggle Tiptap marks",
      "Page settings panel — save title/slug/description changes via API",
      "Table of contents — extracted from headings, updates on content change",
      "Add new dropdown — insert Tab/Dropdown/Anchor/Card/Columns snippets at cursor",
      "Markdown line numbers — scroll sync between gutter and textarea",
      "Large document performance — visual editor with 500+ lines",
      "MDX component rendering in visual mode — Card, Note, Tab components"
    ]
  }
]
