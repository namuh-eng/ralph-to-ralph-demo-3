## Iteration 1 — infra-001: Database Schema
Date: 2026-04-08

### What was built
- Complete Drizzle ORM schema with 10 tables: organizations, org_memberships, projects, deployments, pages, api_keys, agent_jobs, assistant_conversations, analytics_events, audit_logs
- 7 PostgreSQL enums: org_plan, org_role, project_status, deployment_status, api_key_type, agent_job_status, analytics_event_type
- All tables have UUID primary keys, timestamps with timezone, proper foreign keys with cascade deletes
- Unique indexes on org slugs, project slugs per org, page paths per project, user-org membership pairs
- Performance indexes on all foreign key columns and frequently queried fields

### Test results
- 28 unit tests (schema.test.ts) — all passing
- 49 total unit tests — all passing
- make check (typecheck + lint) — passing
- Schema pushed to RDS Postgres via make db-push — success

### Files changed
- src/lib/db/schema.ts — complete schema (was placeholder)
- tests/schema.test.ts — new, 28 tests
- tests/e2e/smoke.spec.ts — new, 2 smoke tests

### Decisions
- Used UUID (defaultRandom) for all primary keys — matches SaaS patterns
- userId in orgMemberships/auditLogs is text (not UUID) to match Better Auth's user.id type
- JSON columns (settings, frontmatter, messages, data, details) typed with $type<> for TypeScript safety
- Cascade deletes on all parent-child relationships (org->projects, project->pages, etc.)

## Iteration 2 — infra-002: S3 File Upload Configuration
Date: 2026-04-08

### What was built
- S3 utility module (`src/lib/s3.ts`) with presigned URL generation for uploads and downloads
- Key builder function following `{orgId}/{projectId}/assets/{filename}` convention
- Upload validation: allowed content types whitelist, path traversal protection, filename validation
- Object lifecycle helpers: delete, exists check
- API route `POST /api/uploads/presign` — generates presigned PUT URL for direct client-to-S3 upload
- API route `GET /api/uploads/presign?key=...` — generates presigned GET URL for downloads
- E2E tests for the presign API endpoint (5 tests)

### Test results
- 16 new unit tests (s3.test.ts) — all passing
- 5 new E2E tests (uploads-api.spec.ts) — written, QA will verify
- 65 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/s3.ts — new, S3 client + presigned URLs + validation
- src/app/api/uploads/presign/route.ts — new, API route for presign
- tests/s3.test.ts — new, 16 unit tests
- tests/e2e/uploads-api.spec.ts — new, 5 E2E tests

### Decisions
- Used allowlist for content types (images, docs, media, fonts) — blocks executables
- 15-minute presigned URL expiry — standard for upload flows
- 50MB max upload size — enforced at validation layer
- Path traversal blocked by rejecting ".." in filenames
- S3 bucket already provisioned by preflight.sh, using env var S3_BUCKET

## Iteration 3 — auth-001: Better Auth with Google OAuth
Date: 2026-04-08

### What was built
- Better Auth server config (`src/lib/auth.ts`) with Google OAuth provider and Drizzle adapter
- Better Auth client (`src/lib/auth-client.ts`) with React hooks (useSession, signIn, signOut)
- Auth API route handler (`src/app/api/auth/[...all]/route.ts`) — handles all auth endpoints
- Login page (`src/app/(auth)/login/page.tsx`) — centered card with Google OAuth button, dark theme
- Signup page (`src/app/(auth)/signup/page.tsx`) — centered card with Google OAuth button, dark theme
- Auth layout (`src/app/(auth)/layout.tsx`) — centered flex container
- Auth middleware (`src/middleware.ts`) — protects /dashboard/*, /settings/*, /products/* routes, redirects to /login
- Better Auth DB schema (`src/lib/db/auth-schema.ts`) — user, session, account, verification tables
- Dashboard placeholder page (`src/app/dashboard/page.tsx`)
- Test-only session creation route (`src/app/api/test/create-session/route.ts`) — NODE_ENV=test guard
- Playwright auth setup fixture (`tests/e2e/auth.setup.ts`) with storage state
- Updated `playwright.config.ts` with auth-setup project and unauthenticated project
- Updated `drizzle.config.ts` to include auth-schema.ts
- Updated `src/lib/db/index.ts` to merge auth schema into Drizzle instance
- Updated layout metadata from "Resend Clone" to "Mintlify Clone"

### Test results
- 8 new unit tests (auth.test.ts) — all passing
- 5 new E2E tests (auth.spec.ts) — written, QA will verify
- 73 total unit tests — all passing
- make check (typecheck + lint) — passing
- Auth schema pushed to RDS Postgres via make db-push — success

### Files changed
- src/lib/auth.ts — new, Better Auth server config
- src/lib/auth-client.ts — new, Better Auth React client
- src/lib/db/auth-schema.ts — new, user/session/account/verification tables
- src/lib/db/index.ts — updated, merged auth schema
- src/app/api/auth/[...all]/route.ts — new, auth API handler
- src/app/(auth)/layout.tsx — new, auth pages layout
- src/app/(auth)/login/page.tsx — new, login page with Google OAuth
- src/app/(auth)/signup/page.tsx — new, signup page with Google OAuth
- src/app/dashboard/page.tsx — new, placeholder
- src/app/api/test/create-session/route.ts — new, E2E test helper
- src/middleware.ts — new, auth middleware
- tests/auth.test.ts — new, 8 unit tests
- tests/e2e/auth.spec.ts — new, 5 E2E tests
- tests/e2e/auth.setup.ts — new, Playwright auth fixture
- playwright.config.ts — updated, auth setup project
- drizzle.config.ts — updated, added auth-schema
- src/app/layout.tsx — updated, metadata
- package.json — added @opentelemetry/api dependency

### Decisions
- Used cookie-based session check in middleware (getSessionCookie from better-auth/cookies) — lightweight, no DB call per request
- Session cookie cache enabled (5 min) for performance
- Login/signup pages share same dark card design with green accent matching Mintlify brand
- Test-only session creation route guarded by NODE_ENV=test — won't respond in production
- Playwright config split into 3 projects: auth-setup, default (authenticated), unauthenticated (auth.spec.ts)
- emailAndPassword not explicitly enabled — Mintlify uses Google OAuth only

## Iteration 4 — auth-002: Organization Creation & Onboarding
Date: 2026-04-08

### What was built
- Org utility module (`src/lib/orgs.ts`) with slugify, validateOrgName, validateCreateOrgRequest functions
- API route `GET /api/orgs` — list orgs the authenticated user belongs to (with role)
- API route `POST /api/orgs` — create org, auto-generate slug, add creator as admin member
- Onboarding page (`src/app/onboarding/page.tsx`) — dark card with org name input, validates on submit, creates org via API, redirects to dashboard
- Middleware updated to protect `/onboarding` route (unauthenticated → /login)
- Login/signup callback URLs changed from `/dashboard` to `/onboarding` (onboarding page auto-skips to dashboard if user already has an org)
- Slug collision handling: appends timestamp suffix if slug already exists

### Test results
- 14 new unit tests (orgs.test.ts: 9, auth.test.ts: +2 for onboarding middleware) — all passing
- 4 new E2E tests (onboarding.spec.ts) — written, QA will verify
- 87 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/orgs.ts — new, slug generation + validation utilities
- src/app/api/orgs/route.ts — new, GET + POST org API routes
- src/app/onboarding/page.tsx — new, org creation onboarding page
- src/middleware.ts — updated, added /onboarding protection
- src/app/(auth)/login/page.tsx — updated, callbackURL → /onboarding
- src/app/(auth)/signup/page.tsx — updated, callbackURL → /onboarding
- tests/orgs.test.ts — new, 9 unit tests
- tests/auth.test.ts — updated, +2 tests for onboarding middleware logic
- tests/e2e/onboarding.spec.ts — new, 4 E2E tests

### Decisions
- OAuth callback redirects to /onboarding instead of /dashboard — onboarding page checks if user has org and skips to /dashboard if so
- Slug generated from org name (lowercase, hyphens, no special chars) — if collision, append base-36 timestamp suffix
- Org creator automatically becomes admin member via orgMemberships insert
- Onboarding page is client component with loading state while checking for existing orgs
- Removed autoFocus from input (Biome accessibility rule)

## Iteration 5 — auth-003: API Key Management
Date: 2026-04-08

### What was built
- API key utility library (`src/lib/api-keys.ts`) with generate, hash (SHA-256), verify, mask, extract prefix, and validation functions
- Key format: admin keys `mint_{32 hex chars}`, assistant keys `mint_dsc_{32 hex chars}`
- API route `GET /api/api-keys` — list all API keys for the user's org (resolves org from session)
- API route `POST /api/api-keys` — create new API key (admin role required), returns raw key once
- API route `DELETE /api/api-keys/[id]` — revoke an API key (admin role required, scoped to org)
- Settings page (`/settings/organization/api-keys`) — dark themed page matching Mintlify design:
  - Two sections: Admin API keys and Assistant API keys
  - Each section shows active keys in a table (name, masked prefix, created date, revoke button)
  - Create button opens modal with name input, returns raw key shown once with copy button
  - Delete button opens confirmation modal before revoking

### Test results
- 22 new unit tests (api-keys.test.ts) — all passing
- 5 new E2E tests (api-keys.spec.ts) — written, QA will verify
- 109 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/api-keys.ts — new, key generation/hashing/validation utilities
- src/app/api/api-keys/route.ts — new, GET + POST API routes
- src/app/api/api-keys/[id]/route.ts — new, DELETE API route
- src/app/settings/organization/api-keys/page.tsx — new, settings page UI
- tests/api-keys.test.ts — new, 22 unit tests
- tests/e2e/api-keys.spec.ts — new, 5 E2E tests

### Decisions
- Used SHA-256 for key hashing — standard, fast, irreversible
- Key prefix stored in DB is prefix + first 8 random chars for identification
- Masked display shows prefix + first 4 random chars + dots
- Admin role required for create/delete operations (checked via orgMemberships)
- Org resolved from user session → first org membership (single-org model for now)
- Raw key shown once in creation modal with copy-to-clipboard button
- API routes at /api/api-keys (not nested under orgs) — org resolved from session context

## Iteration 6 — design-001: Dashboard Layout Shell
Date: 2026-04-08

### What was built
- Sidebar component (`src/components/layout/sidebar.tsx`) — collapsible sidebar with org switcher dropdown, main nav (Home, Editor, Analytics, Settings), Agents group (Agent with "New" badge, Assistant, Workflows, MCP), collapse button
- Top bar component (`src/components/layout/top-bar.tsx`) — search button with ⌘K hint, notifications bell, chat button, profile avatar with Radix dropdown menu (Your profile, Invite members, Billing, Theme switcher, Documentation, Contact support, Log Out)
- Trial banner component (`src/components/layout/trial-banner.tsx`) — amber dismissible banner with free trial info and "Explore upgrades" link
- Dashboard layout client (`src/components/layout/dashboard-layout-client.tsx`) — client wrapper combining sidebar + top bar + trial banner + content area
- Dashboard shell server component (`src/components/layout/dashboard-shell.tsx`) — fetches session and org data, redirects to /login or /onboarding if needed
- Session helper (`src/lib/session.ts`) — server-side session fetcher using Better Auth
- User org helper (`src/lib/get-user-org.ts`) — fetches first org membership for a user
- Dashboard layout (`src/app/dashboard/layout.tsx`) — wraps dashboard pages with DashboardShell
- Settings layout (`src/app/settings/layout.tsx`) — wraps settings pages with DashboardShell
- Products layout (`src/app/products/layout.tsx`) — wraps products pages with DashboardShell
- Updated dashboard page with dynamic greeting ("Good morning/afternoon/evening, {firstName}")
- Active nav item highlighting based on current pathname

### Test results
- 13 new unit tests (dashboard-layout.test.ts) — all passing
- 9 new E2E tests (dashboard-layout.spec.ts) — written, QA will verify
- Updated smoke tests with dashboard and settings page loads
- 122 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/components/layout/sidebar.tsx — new, sidebar with nav groups + org switcher
- src/components/layout/top-bar.tsx — new, top bar with search, notifications, profile menu
- src/components/layout/trial-banner.tsx — new, amber trial banner
- src/components/layout/dashboard-layout-client.tsx — new, client-side layout wrapper
- src/components/layout/dashboard-shell.tsx — new, server-side shell with session/org fetch
- src/components/layout/index.ts — new, barrel export
- src/lib/session.ts — new, server session helper
- src/lib/get-user-org.ts — new, user org lookup helper
- src/app/dashboard/layout.tsx — new, dashboard route layout
- src/app/dashboard/page.tsx — updated, dynamic greeting
- src/app/settings/layout.tsx — new, settings route layout
- src/app/products/layout.tsx — new, products route layout
- tests/dashboard-layout.test.ts — new, 13 unit tests
- tests/e2e/dashboard-layout.spec.ts — new, 9 E2E tests
- tests/e2e/smoke.spec.ts — updated, added dashboard/settings smoke tests
- package.json — added lucide-react, clsx

### Decisions
- Shared DashboardShell server component used across dashboard, settings, and products layouts — avoids code duplication
- Sidebar collapse state managed in client component (DashboardLayoutClient) — not persisted across page reloads
- Active nav detection: exact match for /dashboard, startsWith for other routes
- Profile avatar shows initials (first letter of first+last name) when no image URL available
- Org switcher shows first letter of org name as colored avatar
- Trial banner is dismissible but state resets on page reload (not persisted)
- Used Radix DropdownMenu for profile menu — accessible keyboard navigation built-in
- Used lucide-react for all icons — consistent with Mintlify's icon style
- Dark theme colors: bg-[#0f0f0f] for sidebar/topbar, bg-[#1a1a1a] for elevated surfaces, emerald accents

## Iteration 7 — feature-001: Project CRUD
Date: 2026-04-08

### What was built
- Project utility library (`src/lib/projects.ts`) with slugifyProject, generateSubdomain, validateProjectName, validateCreateProjectRequest, validateUpdateProjectRequest
- API route `GET /api/projects` — list all projects for the user's org
- API route `POST /api/projects` — create a new project with auto-generated slug and subdomain (admin/editor role required)
- API route `GET /api/projects/[id]` — get single project details (org-scoped)
- API route `PUT /api/projects/[id]` — update project name, subdomain, repoUrl, customDomain (admin/editor role required)
- API route `DELETE /api/projects/[id]` — delete a project (admin only, cannot delete last project in org)
- Settings General page (`/settings/deployment/general`) — edit project name and deployment name (subdomain), matches Mintlify dark theme
- New project page (`/dashboard/new-project`) — create documentation project with name and optional GitHub repo URL
- Updated sidebar (`sidebar.tsx`) — project selector in org switcher dropdown, shows list of projects with active indicator, "New documentation" link
- Updated DashboardShell to fetch projects from DB and pass to layout
- Updated DashboardLayoutClient to accept and forward projects prop

### Test results
- 22 new unit tests (projects.test.ts) — all passing
- 5 new E2E tests (projects.spec.ts) — written, QA will verify
- 144 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/projects.ts — new, project slug/validation/subdomain utilities
- src/app/api/projects/route.ts — new, GET + POST project API routes
- src/app/api/projects/[id]/route.ts — new, GET + PUT + DELETE project API routes
- src/app/settings/deployment/general/page.tsx — new, settings general page
- src/app/dashboard/new-project/page.tsx — new, create project page
- src/components/layout/sidebar.tsx — updated, project selector in dropdown
- src/components/layout/dashboard-layout-client.tsx — updated, accepts projects prop
- src/components/layout/dashboard-shell.tsx — updated, fetches projects from DB
- tests/projects.test.ts — new, 22 unit tests
- tests/e2e/projects.spec.ts — new, 5 E2E tests

### Decisions
- Subdomain = `{orgSlug}-{projectSlug}` unless org and project slug are the same (then just project slug)
- Slug collision within org: appends base-36 timestamp suffix (same pattern as org slug collision)
- Admin/editor can create and update projects; only admin can delete
- Cannot delete the org's last project (prevents orphaned org)
- Project selector in sidebar dropdown lists all org projects with active checkmark
- Settings General matches Mintlify screenshot: "Project name" + "Deployment name" fields with save button
- DashboardShell now directly queries DB instead of using getUserOrg helper (needs projects in same query scope)

## Iteration 8 — onboard-001: Onboarding Wizard
Date: 2026-04-08

### What was built
- Transformed single-step org creation page into 4-step onboarding wizard at `/onboarding`
- Step 1: Create organization — name input with validation, creates org via POST /api/orgs
- Step 2: Connect GitHub — optional repository URL input with Skip option, stores URL for project creation
- Step 3: Create project — name input with validation, subdomain preview (live as user types), creates project via POST /api/projects
- Step 4: Success screen — shows created project subdomain, "Go to dashboard" button
- Progress indicator with numbered circles, step labels, and connecting lines showing completion state
- Back button navigation on steps 2 and 3
- Loading spinners during API calls
- Form validation at each step using existing validators (validateOrgName, validateProjectName)

### Test results
- 20 new unit tests (onboarding-wizard.test.ts) — all passing
- 8 new E2E tests (onboarding.spec.ts) — rewritten for wizard flow, QA will verify
- 164 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/app/onboarding/page.tsx — rewritten, multi-step wizard with 4 steps
- tests/onboarding-wizard.test.ts — new, 20 unit tests for wizard step logic
- tests/e2e/onboarding.spec.ts — rewritten, 8 E2E tests for wizard flow

### Decisions
- Reused existing validators (validateOrgName, validateProjectName) and API routes (POST /api/orgs, POST /api/projects) — no new API routes needed
- GitHub step stores repo URL locally and passes it to project creation — no separate GitHub connection API
- Progress indicator uses emerald green for completed steps, matching Mintlify brand
- Subdomain preview shown live on step 3 using generateSubdomain + slugifyProject
- Wizard state is client-side only — refreshing the page restarts the wizard
- Used lucide-react GitBranch icon for GitHub step (no GitHub logo icon in lucide-react)
- Back navigation preserves previously entered data (state not cleared)

## Iteration 9 — feature-002: Page CRUD (Editor)
Date: 2026-04-08

### What was built
- Page utility library (`src/lib/pages.ts`) with normalizePath, titleFromPath, validatePagePath, validatePageTitle, validateCreatePageRequest, validateUpdatePageRequest, buildPageTree
- API route `GET /api/projects/[id]/pages` — list all pages for a project
- API route `POST /api/projects/[id]/pages` — create a new page (path uniqueness enforced per project)
- API route `GET /api/projects/[id]/pages/[pageId]` — get a single page with content
- API route `PUT /api/projects/[id]/pages/[pageId]` — update page title, path, content, description, isPublished
- API route `DELETE /api/projects/[id]/pages/[pageId]` — delete a page
- Editor page (`/editor/main`) — full-featured editor matching Mintlify design:
  - Navigation tab: hierarchical tree view of pages grouped by path segments (folders before files)
  - Files tab: flat list of all pages by path
  - Create page modal with path and title inputs, validation, API submission
  - Delete page confirmation modal
  - Content area: page header with title, path, publish status, delete and save buttons
  - MDX content textarea editor with monospace font
  - Empty state with "Select a page to edit" prompt
- Editor layout (`/editor/layout.tsx`) wraps with DashboardShell
- Updated smoke tests with editor page load test

### Test results
- 30 new unit tests (pages.test.ts) — all passing
- 5 new E2E tests (pages.spec.ts) — written, QA will verify
- 194 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/pages.ts — new, page path/validation/tree utilities
- src/app/api/projects/[id]/pages/route.ts — new, GET + POST page API routes
- src/app/api/projects/[id]/pages/[pageId]/route.ts — new, GET + PUT + DELETE page API routes
- src/app/editor/layout.tsx — new, editor route layout with DashboardShell
- src/app/editor/main/page.tsx — new, editor page with tree sidebar, modals, content editor
- tests/pages.test.ts — new, 30 unit tests
- tests/e2e/pages.spec.ts — new, 5 E2E tests
- tests/e2e/smoke.spec.ts — updated, added editor page smoke test

### Decisions
- Pages API nested under /api/projects/[id]/pages — ensures project scoping
- Path normalization: lowercase, trim slashes, collapse duplicates, convert backslashes
- Page path unique per project — enforced at API level with 409 Conflict response
- Tree built client-side from flat page list using buildPageTree utility
- Folders inferred from path segments, sorted before files alphabetically
- Content editing via plain textarea — rich MDX editor planned for later feature
- Project resolved from first project in user's org (single-project-active model)

## Iteration 10 — feature-003: Dual-mode MDX Editor
Date: 2026-04-08

### What was built
- Editor utility library (`src/lib/editor.ts`) with frontmatter extraction/serialization, line number generation, TOC extraction, auto-save with debounce, MDX component snippets, and cursor-aware snippet insertion
- Visual editor component (`src/components/editor/visual-editor.tsx`) — Tiptap/ProseMirror WYSIWYG with StarterKit, CodeBlock, Link, Underline, Placeholder extensions. Markdown↔HTML conversion for round-trip editing. Prose styles with dark theme (emerald accents)
- Markdown editor component (`src/components/editor/markdown-editor.tsx`) — code editor with line number gutter, scroll sync, Tab key handling (inserts spaces), emerald-tinted syntax coloring
- Editor toolbar component (`src/components/editor/editor-toolbar.tsx`) — Visual/Markdown mode toggle tabs, undo/redo, branch selector (main), formatting buttons (bold, italic, heading, link, image, code block), "Add new" dropdown (Tab, Dropdown, Anchor, Card, Columns), search, page settings gear, preview link, Publish button with Radix Popover showing site URL, auto-save status indicator
- Page settings panel (`src/components/editor/page-settings-panel.tsx`) — slide-in right panel with Title, Slug, External URL, Description, Sidebar title, OG Image URL, Tag toggle, Hidden toggle, Mode selector — matches Mintlify's page settings UI
- Table of contents panel (`src/components/editor/toc-panel.tsx`) — auto-extracted from headings, shows "On this page" with indented heading levels
- Rewrote main editor page (`src/app/editor/main/page.tsx`) — 3-panel layout (left nav/files tree + center dual-mode editor + right TOC/settings), auto-save with 2s debounce, mode toggle preserves content, toolbar formatting inserts at cursor position

### Test results
- 27 new unit tests (editor.test.ts) — all passing
- 7 new E2E tests (editor-dual-mode.spec.ts) — written, QA will verify
- 221 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/editor.ts — new, editor utilities (frontmatter, TOC, auto-save, snippets)
- src/components/editor/visual-editor.tsx — new, Tiptap WYSIWYG visual editor
- src/components/editor/markdown-editor.tsx — new, line-numbered markdown editor
- src/components/editor/editor-toolbar.tsx — new, toolbar with mode toggle, formatting, publish
- src/components/editor/page-settings-panel.tsx — new, page settings side panel
- src/components/editor/toc-panel.tsx — new, table of contents panel
- src/app/editor/main/page.tsx — rewritten, dual-mode editor with 3-panel layout
- tests/editor.test.ts — new, 27 unit tests
- tests/e2e/editor-dual-mode.spec.ts — new, 7 E2E tests
- package.json — added @tiptap/react, @tiptap/starter-kit, @tiptap/pm, @tiptap/extension-* (placeholder, link, code-block, image, underline)

### Decisions
- Used Tiptap (ProseMirror) for visual WYSIWYG — best-in-class contenteditable editor with extension system
- Simple markdown↔HTML converters for round-trip between modes — covers headings, bold, italic, code, links, paragraphs
- Auto-save with 2s debounce — saves after user stops typing, with manual save button as backup
- Toolbar formatting in markdown mode inserts raw markdown at cursor; in visual mode defers to Tiptap commands
- Page settings panel shows on right side when gear icon clicked, replacing TOC panel
- TOC auto-extracted from heading lines in content, shown on right when settings panel is closed
- MDX component snippets (Tab, Dropdown, Anchor, Card, Columns) match Mintlify's "Add new" dropdown
- Branch selector is static "main" for now — git branching feature planned for later
- Publish popover uses Radix Popover component for accessibility

## Iteration 11 — feature-004: MDX Renderer & Docs Site
Date: 2026-04-08

### What was built
- MDX renderer library (`src/lib/mdx-renderer.ts`) with parseMdxToHtml, extractComponentBlocks, renderComponentBlock, renderMdxContent, buildDocsNav, resolvePageFromSlug
- Custom markdown-to-HTML parser: headings with slugified IDs, bold/italic/code/links/images inline, code blocks with language labels + copy buttons, lists, blockquotes, tables (GFM), horizontal rules
- MDX component extraction: parses JSX-like custom components from content, handles nesting (CardGroup > Card), self-closing tags, props parsing
- Component rendering for all Mintlify component types:
  - Callouts: Note, Warning, Tip, Info, Check — with colored borders and SVG icons
  - Cards/CardGroup: grid layout, optional href links, icon + title + description
  - Steps: numbered steps with vertical connector line
  - Tabs: switchable tab panels with active state
  - Accordion/AccordionGroup: details/summary expandable sections
  - CodeGroup: tabbed code blocks with language tabs
  - Frame: figure with optional caption
  - Columns/Column: CSS grid layout
  - Dropdown: alias for accordion
- Docs site layout (`/docs/[subdomain]/[...slug]`): sticky top bar with logo + search, left sidebar nav (260px) with grouped pages, center content area (768px max), right TOC panel (220px) with scroll spy
- Docs sidebar component (`docs-sidebar.tsx`): collapsible nav groups, active page highlighting (green)
- Docs TOC component (`docs-toc.tsx`): auto-generated from headings, IntersectionObserver for active tracking
- Docs pagination component (`docs-pagination.tsx`): prev/next page links at bottom
- MDX content client component (`mdx-content.tsx`): wires tab switching, code copy interactivity via event listeners
- Docs index page (`/docs/[subdomain]`): redirects to first published page
- Docs API route (`GET /api/docs/[subdomain]`): returns project + all published pages
- Full CSS styles in globals.css for all docs components matching Mintlify dark theme

### Test results
- 40 unit tests (mdx-renderer.test.ts) — all passing
- 5 E2E tests (docs-site.spec.ts) — written, QA will verify
- 261 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/mdx-renderer.ts — new, MDX rendering engine
- src/components/docs/mdx-content.tsx — new, client-side MDX content with interactivity
- src/components/docs/docs-sidebar.tsx — new, docs site sidebar navigation
- src/components/docs/docs-toc.tsx — new, table of contents with scroll spy
- src/components/docs/docs-pagination.tsx — new, prev/next page links
- src/app/docs/[subdomain]/layout.tsx — new, docs layout
- src/app/docs/[subdomain]/page.tsx — new, docs index redirect
- src/app/docs/[subdomain]/[...slug]/page.tsx — new, docs page with full layout
- src/app/api/docs/[subdomain]/route.ts — new, docs API endpoint
- src/app/globals.css — updated, added comprehensive docs site + component styles
- tests/mdx-renderer.test.ts — new, 40 unit tests
- tests/e2e/docs-site.spec.ts — new, 5 E2E tests

### Decisions
- Custom markdown parser (no external dependency) — keeps bundle small, handles all standard markdown + GFM tables
- Component extraction via regex-based parser — handles nested components, self-closing tags, JSX-like props
- Docs navigation auto-generated from page paths — root pages become items, pages with path segments form groups
- Dark theme matching Mintlify: #090d0d background, #16a34a green accents, Inter font, JetBrains Mono for code
- Content rendered server-side as HTML, client component adds interactivity (tabs, copy buttons)
- Scroll spy for TOC uses IntersectionObserver with offset margins
- Docs site served at /docs/[subdomain]/[...slug] — not separate domain, accessible within same app

## Iteration 12 — feature-004a: MDX Component Library Enhancement
Date: 2026-04-08

### What was built
- SVG icon map for Card components — 15 common icons (pencil, star, code, palette, terminal, book, rocket, settings, globe, compass, newspaper, layout, file-text, users, search, edit-3) rendered as inline SVG instead of plain text
- Callout title prop support — Note, Warning, Tip, Info, Check callouts now accept a `title` prop rendered as bold heading above body content
- Code block enhancements: filename support (```lang filename syntax), Ask AI button alongside copy button, syntax highlighting for keywords/strings/comments
- Syntax highlighting engine — regex-based highlighter for keywords (const, function, class, etc.), double/single-quoted strings, single-line comments (// and #), with safe HTML tag boundary detection
- CodeGroup enhancement — copy buttons added to each code panel
- Client-side interactivity — Ask AI buttons dispatch custom `ask-ai-code` events with code content and language; copy buttons work in both code-block and code-group contexts
- CSS enhancements — code-actions layout, code-ask-ai button (green accent), code-panel-actions, callout-title styling, card-icon SVG sizing, syntax highlighting colors (purple keywords, green strings, gray italic comments)

### Test results
- 26 new unit tests (mdx-components.test.ts) — all passing
- 5 new E2E tests (mdx-components.spec.ts) — written, QA will verify
- 1 existing test updated (mdx-renderer.test.ts — code block test updated for syntax highlighting)
- 287 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/mdx-renderer.ts — enhanced: SVG icon map, syntax highlighting engine, callout title prop, filename parsing, Ask AI button, CodeGroup copy buttons
- src/components/docs/mdx-content.tsx — enhanced: Ask AI event dispatching, copy button for code-group panels
- src/app/globals.css — enhanced: code-actions, code-ask-ai, code-panel-actions, callout-title, card-icon SVG, syntax highlighting classes
- tests/mdx-components.test.ts — new, 26 unit tests
- tests/e2e/mdx-components.spec.ts — new, 5 E2E tests
- tests/mdx-renderer.test.ts — updated, 1 test adjusted for syntax highlighting

### Decisions
- SVG icons stored as inline SVG strings in a lookup map — avoids external icon library dependency, keeps bundle small
- Unknown icon names fall back to text display (not empty) — graceful degradation
- Syntax highlighting is regex-based with HTML tag boundary detection — splits content by `<[^>]+>` tags and only applies keyword replacement to text nodes
- Keywords set covers JS/TS, Python, Go, Java common keywords — intentionally broad but not language-specific
- Ask AI button dispatches a CustomEvent ("ask-ai-code") rather than direct navigation — allows parent component to handle the interaction (e.g., open assistant panel)
- Code block filename parsed from meta string: ```lang filename — matches common MDX convention

## Iteration 13 — feature-005: Deployment System
Date: 2026-04-08

### What was built
- Deployment utility library (`src/lib/deployments.ts`) with status helpers (label, color, dot color), timeAgo formatter, shortSha, validation for trigger requests, deployment log step generation, duration calculation, and duration formatting
- API route `GET /api/deployments` — list deployments for the user's active project (max 50, newest first)
- API route `POST /api/deployments` — trigger a new deployment (admin/editor role required), simulates async build that completes after 3s
- API route `GET /api/deployments/[id]` — get single deployment with org-scoped access check
- API route `POST /api/deployments/[id]/complete` — update deployment status to succeeded/failed, updates project status accordingly
- Dashboard home page rewritten with Mintlify-matching UI:
  - Project overview card: project name, Live/Updating/Error status badge, last deployment info, Visit site link, domain info, "Add custom domain" link
  - Trigger deployment button (rocket icon) and refresh button
  - Activity section with Live/Previews tab toggle
  - Deployment table: Update (name + time ago), Status (colored badge), Changes (commit message + SHA)
  - Expandable deployment rows: commit details (source, SHA), deployment log with checkmark steps
  - Empty states for no deployments and no project

### Test results
- 30 new unit tests (deployments.test.ts) — all passing
- 5 new E2E tests (deployments.spec.ts) — written, QA will verify
- 317 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/deployments.ts — new, deployment utilities
- src/app/api/deployments/route.ts — new, GET + POST deployment API routes
- src/app/api/deployments/[id]/route.ts — new, GET single deployment
- src/app/api/deployments/[id]/complete/route.ts — new, POST status update
- src/app/dashboard/page.tsx — rewritten, server component fetching project + deployments
- src/app/dashboard/dashboard-home-client.tsx — new, full dashboard home UI
- tests/deployments.test.ts — new, 30 unit tests
- tests/e2e/deployments.spec.ts — new, 5 E2E tests

### Decisions
- Build completion simulated via setTimeout (3s) — in production would be a background job/queue
- Project status updated to "deploying" on trigger, then "active" on success or "error" on failure
- Deployment table limited to 50 rows — pagination not needed for MVP
- Expanded row shows commit details (source ref, SHA) + deployment log steps matching Mintlify screenshots
- Preview tab is placeholder — branch-based preview deployments planned for later feature
- resolveUserProject helper extracts common org→project resolution pattern used across deployment routes

## Iteration 14 — feature-006: Dashboard Overview Quick Actions
Date: 2026-04-08

### What was built
- Dashboard utility library (`src/lib/dashboard.ts`) with buildQuickActionCards, buildSiteUrl, formatDomainDisplay, projectStatusSummary
- Quick action cards row on dashboard: 4 cards linking to Editor, View Site, Analytics, Settings
- Each card has icon (lucide-react), title, description, hover effect with emerald accent
- View Site card opens docs in new tab (target="_blank")
- Site URL in domain section now clickable link opening docs in new tab
- data-testid attributes added for E2E testability (project-status-badge, quick-action-card, site-url-link)
- Domain display uses formatDomainDisplay helper (customDomain or subdomain.mintlify.app)
- Site URL uses buildSiteUrl helper (custom domain with https or /docs/subdomain path)

### Test results
- 17 new unit tests (dashboard.test.ts) — all passing
- 5 new E2E tests (dashboard-overview.spec.ts) — written, QA will verify
- 334 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/dashboard.ts — new, dashboard overview utilities
- src/app/dashboard/dashboard-home-client.tsx — enhanced, quick action cards + data-testid + site URL links
- tests/dashboard.test.ts — new, 17 unit tests
- tests/e2e/dashboard-overview.spec.ts — new, 5 E2E tests

### Decisions
- Quick action cards rendered as 4-column grid below project overview, above activity section
- View Site card uses <a> tag for external link; other cards use Next.js <Link> for client navigation
- Icon map (edit→Edit3, globe→Globe, settings→Settings, bar-chart→BarChart3) keeps icon rendering in one place
- Analytics card links to /analytics (page to be built in future feature)
- buildQuickActionCards returns static card definitions; view-site href filled dynamically from project subdomain/customDomain

## Iteration 7 — feature-014: Docs Site Layout
Date: 2026-04-08

### What was built
- **Theme toggle**: Light/dark mode with localStorage persistence, ThemeProvider context, ThemeToggle button (sun/moon icons). Defaults to dark. Applied via `data-theme` attribute on `<html>`.
- **Search modal**: Full overlay with input, filters pages by title/path (case-insensitive). Opens via ⌘K / Ctrl+K keyboard shortcut or clicking search button. Closes with Escape or clicking overlay. Results link to doc pages.
- **Mobile responsive**: Hamburger menu button (hidden on desktop, visible ≤768px). MobileSidebar overlay with full nav, closes on page navigation or Escape. Body scroll locked when open.
- **DocsTopbar**: Extracted topbar into dedicated client component integrating search button, theme toggle, mobile menu, and logo link.
- **Layout polish**: TOC width updated 220→240px per spec. Responsive breakpoints: ≤1024px hides TOC, ≤768px hides sidebar and shows hamburger. Search text/kbd hidden on mobile.
- **Light theme CSS**: Complete light mode styles for all docs components (content prose, cards, callouts, code blocks, tabs, accordions, search modal, mobile sidebar, pagination, TOC).
- **Layout constants**: Exported LAYOUT_WIDTHS (sidebar: 260, toc: 240, maxContent: 1440) for consistency.

### Test results
- 12 new unit tests (docs-site-layout.test.ts) — all passing
- 8 new E2E tests (docs-site-layout.spec.ts) — written, QA will verify
- 346 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/components/docs/theme-provider.tsx — new, ThemeProvider + ThemeToggle + storage helpers
- src/components/docs/search-modal.tsx — new, SearchModal + filterPages + handleSearchShortcut
- src/components/docs/mobile-nav.tsx — new, MobileMenuButton + MobileSidebar
- src/components/docs/docs-topbar.tsx — new, extracted DocsTopbar with all top bar UI
- src/components/docs/layout-constants.ts — new, LAYOUT_WIDTHS export
- src/app/docs/[subdomain]/layout.tsx — updated, wraps children in ThemeProvider
- src/app/docs/[subdomain]/[...slug]/page.tsx — updated, uses DocsTopbar, SearchModal, MobileSidebar
- src/app/globals.css — updated, theme toggle styles, search modal styles, mobile nav styles, responsive breakpoints, complete light theme CSS, TOC width 220→240px
- tests/docs-site-layout.test.ts — new, 12 unit tests
- tests/e2e/docs-site-layout.spec.ts — new, 8 E2E tests

### Decisions
- Theme stored in localStorage (key: "docs-theme"), applied via data-theme attribute on html element for CSS specificity
- Used native <dialog> element for search modal (Biome a11y compliance)
- Mobile sidebar communicates via CustomEvents (toggle-mobile-sidebar, open-search) to avoid prop drilling between server/client components
- Responsive breakpoints: 768px (mobile — sidebar hides, hamburger shows), 1024px (tablet — TOC hides)

## Iteration 16 — feature-014a: Docs Site Top Bar
Date: 2026-04-08

### What was built
- Enhanced DocsTopbar component (`src/components/docs/docs-topbar.tsx`) with full navigation bar matching Mintlify's design:
  - Logo + project name on left (existing, kept as-is)
  - Search button (⌘K) moved to center section alongside new Ask AI button
  - Ask AI button with sparkle icon — dispatches `toggle-ask-ai` custom event
  - Support link (mailto) — uses project settings `supportEmail` or default
  - GitHub icon link — conditionally rendered when `githubUrl` in project settings, opens in new tab
  - Dashboard button (green) — links to /dashboard with arrow icon
  - Theme toggle (existing, repositioned to right section)
  - Mobile hamburger menu (existing, unchanged)
- Exported helper functions: `buildDashboardUrl`, `buildSupportHref`, `getGithubLinkProps`, `AskAiButton`
- DocsTopbar now accepts optional `settings` prop from project settings (JSONB)
- Updated `[...slug]/page.tsx` to pass `project.settings` to DocsTopbar
- Added CSS for all new elements: dark theme (default), light theme overrides
- Responsive: on mobile (≤768px), Ask AI text hides, Support link hides, Dashboard button shrinks

### Test results
- 13 new unit tests (docs-topbar.test.ts) — all passing
- 6 new E2E tests (docs-topbar.spec.ts) — written, QA will verify
- 359 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/components/docs/docs-topbar.tsx — enhanced, added Ask AI, Support, GitHub, Dashboard
- src/app/docs/[subdomain]/[...slug]/page.tsx — updated, passes settings to DocsTopbar
- src/app/globals.css — added CSS for topbar-center, ask-ai, link, icon-link, dashboard-btn + light theme + responsive
- tests/docs-topbar.test.ts — new, 13 unit tests
- tests/e2e/docs-topbar.spec.ts — new, 6 E2E tests

### Decisions
- Search + Ask AI grouped in center section (matching Mintlify's layout from screenshot)
- Ask AI dispatches custom event — actual AI panel will be built in a separate feature
- GitHub link uses sr-only span for Biome a11y compliance (useAnchorContent rule)
- Settings prop is optional and defaults to empty object — backwards compatible
- Dashboard button styled as green pill matching Mintlify's brand accent (#16a34a)
- Support defaults to mailto:support@example.com when no email configured

## Iteration 7 — api-001: REST API Deployment Endpoints
Date: 2026-04-08

### What was built
- Utility module (`src/lib/api-v1-deployments.ts`) — extractBearerToken, validateProjectId, formatDeploymentTriggerResponse, formatDeploymentStatusResponse
- API key auth module (`src/lib/api-key-auth.ts`) — validates Bearer token against hashed API keys in DB, updates lastUsedAt
- API route `POST /api/v1/project/update/{projectId}` — trigger deployment via admin API key, returns statusId + status
- API route `GET /api/v1/project/update-status/{statusId}` — check deployment progress, returns full status info
- Both routes enforce admin-only API key auth (Bearer token)
- Project ownership verified: key's org must match project's org
- Simulated async build (queued → in_progress → succeeded over 3s)

### Test results
- 12 new unit tests (api-v1-deployments.test.ts) — all passing
- 5 new E2E tests (api-v1-deployments.spec.ts) — written, QA will verify
- 371 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/api-v1-deployments.ts — new, request/response formatting utilities
- src/lib/api-key-auth.ts — new, API key Bearer token authentication
- src/app/api/v1/project/update/[projectId]/route.ts — new, trigger deployment endpoint
- src/app/api/v1/project/update-status/[statusId]/route.ts — new, deployment status endpoint
- tests/api-v1-deployments.test.ts — new, 12 unit tests
- tests/e2e/api-v1-deployments.spec.ts — new, 5 E2E tests
- prd.json — api-001 build_pass set to true
- qa-hints.json — added api-001 QA hints

### Decisions
- Validate projectId format (UUID) before auth to give clear 400 errors
- Only admin keys can trigger deployments (assistant keys get 403)
- Cross-org isolation: project must belong to the API key's org
- Reused existing hashApiKey/verifyApiKey from api-keys.ts for key lookup
- lastUsedAt updated fire-and-forget on successful auth (non-blocking)
- Build simulation matches existing dashboard deployment flow (3s completion)

## Iteration 8 — api-002: REST API Assistant Endpoints
Date: 2026-04-08

### What was built
- Assistant helper module (`src/lib/assistant.ts`) with validation and formatting functions:
  - `validateCreateMessageRequest` — validates fp, messages array, optional threadId/retrievalPageSize/currentPath
  - `validateSearchRequest` — validates query, pageSize (1-50, default 10), scoreThreshold
  - `validateGetPageContentRequest` — validates and normalizes path (strips slashes)
  - `buildSearchQuery` — builds ILIKE pattern with SQL wildcard escaping
  - `formatSearchResult` — formats page into search result with truncated content snippet (500 chars)
  - `formatPageContentResponse` — formats page content response
- Three API routes:
  - `POST /api/v1/assistant/create-message` — streams AI responses via AWS Bedrock (Claude Sonnet 4), retrieves relevant published pages as context, persists conversation to DB, sends SSE events (text-delta, sources, finish)
  - `POST /api/v1/assistant/search` — keyword search across published pages using ILIKE on title/content/path, returns results with content snippets and metadata
  - `POST /api/v1/assistant/get-page-content` — retrieves full page content by path, returns 404 if not found or unpublished
- All endpoints use assistant API key auth (Bearer token), org-scoped (only returns pages from the key's org)
- Installed `@aws-sdk/client-bedrock-runtime` for Bedrock streaming

### Test results
- 26 new unit tests (assistant-api.test.ts) — all passing
- 6 new E2E tests (assistant-api.spec.ts) — written, QA will verify
- 397 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/assistant.ts — new, validation + formatting helpers
- src/app/api/v1/assistant/create-message/route.ts — new, streaming AI endpoint
- src/app/api/v1/assistant/search/route.ts — new, search endpoint
- src/app/api/v1/assistant/get-page-content/route.ts — new, page content endpoint
- tests/assistant-api.test.ts — new, 26 unit tests
- tests/e2e/assistant-api.spec.ts — new, 6 E2E tests
- package.json — added @aws-sdk/client-bedrock-runtime
- prd.json — api-002 build_pass set to true
- qa-hints.json — added api-002 QA hints

### Decisions
- Used POST for all 3 endpoints (matches target Mintlify API shape from discovery-openapi.json)
- Bedrock model: us.anthropic.claude-sonnet-4-20250514 with temperature 0.3 and maxTokens 2048
- Search uses ILIKE keyword matching (not semantic/vector) — sufficient for initial version
- Page context limited to retrievalPageSize (default 5, max 20) with 2000 char per page
- Conversation persisted fire-and-forget after streaming completes
- SSE format: data: JSON\n\n with types text-delta, sources, finish, error

## Iteration 9 — feature-007: Analytics Layout Shell
Date: 2026-04-08

### What was built
- Analytics utility library (`src/lib/analytics.ts`) with date range presets, traffic source types, tab configs, and formatting helpers
- 9 date presets: Today, Yesterday, Last 7/14/30/90 days, This month, Year to date, All time
- Traffic source toggle: Humans (5 tabs: Visitors, Views, Assistant, Searches, Feedback) / Agents (2 tabs: Agent Visitors, MCP Searches)
- Analytics shell component (`src/app/analytics/analytics-shell.tsx`) — full client component with:
  - Humans/Agents pill toggle with data-active attribute
  - Date range picker button showing formatted range → popover with presets sidebar + calendar grid
  - Calendar with month navigation, day selection (start/end range), today highlight, range highlight
  - Sub-tab navigation as links with optional count badges
  - URL preserves trafficSource, from, to as query params
- Analytics page (`src/app/analytics/page.tsx`) with Suspense wrapper
- Analytics layout (`src/app/analytics/layout.tsx`) using DashboardShell
- Middleware updated to protect /analytics routes
- Smoke tests updated with analytics page load check

### Test results
- 21 new unit tests (analytics.test.ts) — all passing
- 7 new E2E tests (analytics.spec.ts) — written, QA will verify
- 418 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/analytics.ts — new, date presets + traffic source + tab configs + helpers
- src/app/analytics/layout.tsx — new, DashboardShell wrapper
- src/app/analytics/page.tsx — new, analytics page with Suspense
- src/app/analytics/analytics-shell.tsx — new, client component with toggle/picker/tabs
- src/middleware.ts — updated, added /analytics to protected routes
- tests/analytics.test.ts — new, 21 unit tests
- tests/e2e/analytics.spec.ts — new, 7 E2E tests
- tests/e2e/smoke.spec.ts — updated, added analytics smoke test

### Decisions
- Date range defaults to "Last 7 days" when no URL params present
- Traffic source defaults to "human" for any non-"agent" value
- Calendar grid uses unique date-based keys (year-month-day) to avoid array index keys
- Popover closes on click-outside using mousedown event listener
- Tab hrefs preserve existing query params (trafficSource, from, to) when navigating
- Used emerald-600 for selected date range highlighting, matching Mintlify green accent
- Sub-tabs use border-bottom-2 active indicator (solid white when active)
- Switching traffic source resets to /analytics base path (first tab of new mode)

## Iteration 11 — feature-007a: Analytics Visitors Tab
Date: 2026-04-09

### What was built
- Analytics visitors utility library (`src/lib/analytics-visitors.ts`) with types (DailyVisitorCount, TopPage, Referral, VisitorsData), date range generator, date key formatter, path truncation, daily count gap-filler, chart date formatter
- API route `GET /api/analytics/visitors?projectId=&from=&to=` — aggregates analytics_events (type=view) into daily counts, top pages (by data->>'page'), and referrals (by data->>'referrer'), with auth + org membership verification
- Visitors tab page (`/analytics`) — full implementation with:
  - "Visitors Over Time" heading with total count
  - Recharts ComposedChart: bar chart (emerald fill) + line overlay (emerald stroke) with daily visitor counts
  - Interactive tooltip (dark theme), grid lines, date axis labels
  - Top pages table: page path (truncated with title tooltip) + views count, sorted desc
  - Referrals table: source name (e.g., "$direct") + views count, sorted desc
  - Tables displayed side-by-side (2-column grid on md+)
  - Loading skeleton state with animated pulse placeholders
  - Agent mode empty state: "No visitor activity" with icon
- Installed Recharts dependency
- Date range from URL params flows through to API calls; missing dates filled with zero counts

### Test results
- 18 new unit tests (analytics-visitors.test.ts) — all passing
- 6 new E2E tests (analytics-visitors.spec.ts) — written, QA will verify
- 436 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/analytics-visitors.ts — new, visitor data types and utility functions
- src/app/api/analytics/visitors/route.ts — new, analytics visitors API endpoint
- src/app/analytics/page.tsx — rewritten, visitors tab with chart and tables (was placeholder)
- tests/analytics-visitors.test.ts — new, 18 unit tests
- tests/e2e/analytics-visitors.spec.ts — new, 6 E2E tests
- package.json — added recharts dependency

### Decisions
- Used Recharts ComposedChart (Bar + Line overlay) for "Visitors Over Time" — matches Mintlify's interactive SVG chart style
- Page path from data->>'page' JSONB field, referrer from data->>'referrer' — empty/null referrer defaults to "$direct"
- Daily counts gap-filled client-side (fillDailyCounts) so chart always shows continuous date axis
- Project resolved via /api/projects (first project in org) — same pattern as editor page
- Tables limited to top 20 entries to keep UI clean
- Agent mode shows dedicated empty state rather than empty chart

## Iteration 11 — feature-007b: Analytics Views Tab
Date: 2026-04-09

### What was built
- Analytics Views tab page (`/analytics/views`) — "Page Views Over Time" chart + Top pages table (no Referrals table)
- API route `GET /api/analytics/views?projectId=...&from=...&to=...` — returns dailyCounts, topPages, totalViews
- Analytics views utility module (`src/lib/analytics-views.ts`) — ViewsData type, DailyViewCount alias, formatViewCount, sumDailyCounts helpers
- Same Recharts ComposedChart (Bar + Line) as Visitors tab but labeled "Page Views"
- Agent mode shows "No page view activity" empty state with book icon
- Loading skeleton with single table placeholder (no 2-column grid since no referrals)
- Reuses existing analytics-visitors utilities: fillDailyCounts, formatChartDate, generateDateRange, truncatePath

### Test results
- 11 new unit tests (analytics-views.test.ts) — all passing
- 5 new E2E tests (analytics-views.spec.ts) — written, QA will verify
- 447 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/analytics-views.ts — new, ViewsData type + formatViewCount + sumDailyCounts
- src/app/api/analytics/views/route.ts — new, GET endpoint for page view analytics
- src/app/analytics/views/page.tsx — new, Views tab with chart + top pages table
- tests/analytics-views.test.ts — new, 11 unit tests
- tests/e2e/analytics-views.spec.ts — new, 5 E2E tests

### Decisions
- Reused DailyVisitorCount type as DailyViewCount alias — same shape, different semantic meaning
- API route mirrors visitors route structure but omits referrals query — Views tab focuses only on page views
- Single Top pages table (full width) instead of 2-column grid — matches PRD spec ("no Referrals table")
- Agent empty state uses book icon (svg) instead of people icon to differentiate from visitors

## Iteration 12 — feature-008: AI Assistant Chat Widget
Date: 2026-04-09

### What was built
- Chat widget component (`src/components/docs/chat-widget.tsx`) — fixed-bottom chat panel on docs site with:
  - Toggle via "Ask AI" button (listens for `toggle-ask-ai` custom event from topbar)
  - Header with AI sparkle icon, clear conversation button, close button
  - Scrollable message area with user/assistant messages, typing indicators
  - Source citations as green linked badges below assistant messages
  - Empty state with prompt to ask a question
  - Textarea input with Enter-to-send (Shift+Enter for newline)
  - Green send button with disabled state during streaming
  - Disclaimer: "Responses are generated using AI and may contain mistakes."
  - Auto-scroll to bottom on new messages
  - Auto-focus input when panel opens
- Public docs chat API route (`/api/docs/[subdomain]/chat`) — streams AI responses using AWS Bedrock:
  - Scoped by subdomain — looks up project, no API key needed
  - Retrieves relevant published pages via ILIKE search
  - Streams SSE events: text-delta, sources, finish, error
  - Persists conversations to assistantConversations table (fire-and-forget)
  - Uses Claude Sonnet 4 via Bedrock ConverseStreamCommand
- Chat state managed via useReducer with typed actions (TOGGLE, ADD_USER_MESSAGE, START_STREAMING, APPEND_DELTA, SET_SOURCES, FINISH_STREAMING, STREAM_ERROR, CLEAR)
- Client-side SSE parsing reads streaming response and dispatches actions
- Full CSS styles for dark theme with light theme overrides
- Integrated into docs site page (`/docs/[subdomain]/[...slug]`)

### Test results
- 20 new unit tests (assistant-chat.test.ts) — all passing
- 5 new E2E tests (assistant-chat.spec.ts) — written, QA will verify
- 467 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/components/docs/chat-widget.tsx — new, chat widget with SSE streaming
- src/app/api/docs/[subdomain]/chat/route.ts — new, public docs chat API
- src/app/docs/[subdomain]/[...slug]/page.tsx — updated, added ChatWidget
- src/app/globals.css — updated, added chat widget styles + light theme overrides
- tests/assistant-chat.test.ts — new, 20 unit tests
- tests/e2e/assistant-chat.spec.ts — new, 5 E2E tests

### Decisions
- Created separate public chat API (/api/docs/[subdomain]/chat) rather than requiring API key auth — docs site visitors shouldn't need API keys
- Chat panel is fixed at bottom of page (not floating overlay) — matches Mintlify's inline chat bar design from build-spec
- State managed via useReducer for predictable state transitions during streaming
- SSE parsing handles text-delta, sources, finish, and error event types
- Conversation persisted fire-and-forget to avoid blocking the stream
- Source links point to /docs/[subdomain]/[path] for in-site navigation
- AbortController used for fetch cancellation on unmount

## Iteration 14 — feature-014b: Docs Page Chrome
Date: 2026-04-09

### What was built
- Page chrome utility library (`src/lib/page-chrome.ts`) with getGroupName, titleize, pageToMarkdown, slugifyHeading
- Page header actions client component (`src/components/docs/page-chrome.tsx`):
  - Copy page button: copies full page as markdown (H1 title + content) to clipboard, shows checkmark confirmation for 2s
  - More actions kebab dropdown with "Copy link" (copies current URL) and "View source" options
  - Dropdown closes on outside click
- Heading anchors client component (`HeadingAnchors`):
  - Injects anchor link icon spans into all heading anchors after mount
  - Icon appears on hover via CSS (inline SVG link icon in green)
  - Click handler: smooth scrolls to heading and updates URL hash
- Breadcrumb refactored: now shows group name (parent path segment, titleized) in green text above H1 instead of full path breadcrumb trail. Only appears for nested pages (pages in groups).
- Title row layout: H1 title + action buttons in flex row with space-between
- Full CSS for dark and light themes: action buttons, dropdown menu, heading anchor icon
- Light theme overrides for all new elements

### Test results
- 15 new unit tests (page-chrome.test.ts) — all passing
- 5 new E2E tests (page-chrome.spec.ts) — written, QA will verify
- 482 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/page-chrome.ts — new, page chrome utilities
- src/components/docs/page-chrome.tsx — new, PageHeaderActions + HeadingAnchors client components
- src/app/docs/[subdomain]/[...slug]/page.tsx — updated, uses new breadcrumb group name, title row with actions, heading anchors
- src/app/globals.css — updated, added page chrome CSS (title row, action buttons, dropdown, heading anchor icon, light theme)
- tests/page-chrome.test.ts — new, 15 unit tests
- tests/e2e/page-chrome.spec.ts — new, 5 E2E tests

### Decisions
- Breadcrumb simplified to show only group name (parent segment) in green, not full path trail — matches Mintlify's "group name above title" pattern
- Heading anchor icons injected client-side via DOM manipulation (HeadingAnchors component) since HTML is server-rendered
- Copy page uses pageToMarkdown helper to reconstruct clean markdown (H1 + content)
- More actions dropdown uses click-outside pattern with useRef for closing
- Anchor click uses smooth scroll + replaceState for clean URL hash updates without page jump
- SVG link icon encoded as data URI in CSS to avoid extra network requests

## Iteration 15 — feature-014c: Docs Site TOC (Table of Contents)
Date: 2026-04-09

### What was built
- Enhanced `DocsToc` component to filter entries to H2 and H3 headings only (per feature spec)
- Added smooth scroll on TOC link click via `scrollIntoView({ behavior: "smooth", block: "start" })`
- URL hash updated on click via `history.pushState` without page jump
- Active heading highlighted via IntersectionObserver with rootMargin "-20% 0% -80% 0%"
- Padding adjusted: H2 at 8px, H3 at 20px (uses `(level - 2) * 12 + 8`)
- Added `data-testid` attributes for E2E testability (docs-toc, toc-link-{id})
- Sticky positioning at top: 88px (below topbar + breadcrumb)
- Hidden on viewports ≤1024px via CSS media query

### Test results
- 11 new unit tests (docs-toc.test.ts) — all passing
- 5 new E2E tests (docs-toc.spec.ts) — written, QA will verify
- 493 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/components/docs/docs-toc.tsx — enhanced: H2/H3 filter, smooth scroll, data-testid, padding fix
- tests/docs-toc.test.ts — new, 11 unit tests
- tests/e2e/docs-toc.spec.ts — new, 5 E2E tests

### Decisions
- Filtered to H2/H3 in the component (not in extractToc) so the extraction function remains general-purpose for other consumers
- Smooth scroll uses native scrollIntoView API — no polyfill needed for modern browsers
- pushState used instead of replaceState to preserve back-button navigation through anchors
- Padding relative to level-2 baseline (H2=8px, H3=20px) for clean visual hierarchy

## Iteration 15 — feature-016: Docs Site Search
Date: 2026-04-09

### What was built
- Full-text search library (`src/lib/search.ts`) with tsquery builder (prefix matching), snippet extraction (markdown stripping, query-centered window), HTML-safe highlight, breadcrumb generation, and result grouping by section
- Public search API endpoint (`GET /api/docs/[subdomain]/search?q=...&limit=...`) — no auth required, ILIKE matching on title/description/content/path with relevance-based ordering (title > description > path > content), returns results with snippets and breadcrumbs
- Upgraded search modal (`src/components/docs/search-modal.tsx`) — API-backed search with 200ms debounce, client-side fallback on API error, abort controller for request cancellation, keyboard navigation (arrow up/down, Enter), recent searches from localStorage (max 5), results grouped by section with title/snippet/breadcrumb, footer with keyboard hints
- CSS styles for new search elements: section titles, result content layout with snippet, active result highlight, loading state, recent searches, footer with keyboard hints, light theme variants

### Test results
- 23 new unit tests (search.test.ts) — all passing
- 5 new E2E tests (docs-search.spec.ts) — written, QA will verify
- 516 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/search.ts — new, search utilities (tsquery, snippet, highlight, breadcrumb, grouping)
- src/app/api/docs/[subdomain]/search/route.ts — new, public search API endpoint
- src/components/docs/search-modal.tsx — rewritten, API-backed search with full UX
- src/app/globals.css — updated, added search modal section/snippet/footer/light theme styles
- tests/search.test.ts — new, 23 unit tests
- tests/e2e/docs-search.spec.ts — new, 5 E2E tests

### Decisions
- Used ILIKE with relevance ordering (CASE WHEN) instead of Postgres tsvector — simpler, works without schema migration, good enough for docs-scale data
- buildTsQuery still built for future tsvector upgrade — adds :* prefix matching for type-ahead
- 200ms debounce on search input — balances responsiveness with API load
- Client-side fallback (filterPages) when API fails — graceful degradation
- Recent searches stored in localStorage (max 5) — persists across sessions
- Results grouped by first path segment — matches Mintlify's section-based layout
- Snippets strip markdown and center around first query match (160 char window)

## Iteration 16 — api-003: REST API Analytics Export Endpoints
Date: 2026-04-09

### What was built
- Shared analytics export utility library (`src/lib/analytics-export.ts`) with parseExportParams, paginatedResponse, isValidDateString — handles dateFrom/dateTo date parsing, limit/offset pagination with defaults (100/0) and max (1000)
- 7 REST API export endpoints under `/api/v1/analytics/`, all with admin API key auth:
  1. `GET /api/v1/analytics/views` — daily page view counts, paginated
  2. `GET /api/v1/analytics/visitors` — daily visitor counts with top page and referrer per day
  3. `GET /api/v1/analytics/feedback` — individual feedback events (page, rating, comment)
  4. `GET /api/v1/analytics/searches` — search queries aggregated by count
  5. `GET /api/v1/analytics/assistant-conversations` — full conversation exports with messages
  6. `GET /api/v1/analytics/assistant-caller-stats` — daily conversation count, total/avg messages
  7. `GET /api/v1/analytics/feedback-by-page` — feedback aggregated per page (helpful/not helpful counts)
- All endpoints: Bearer admin API key auth, projectId ownership check, dateFrom/dateTo date filtering, limit/offset pagination, JSON response envelope `{ data, pagination: { total, limit, offset } }`

### Test results
- 13 new unit tests (analytics-export.test.ts) — all passing
- 21 new E2E tests (analytics-export.spec.ts) — written, QA will verify
- 529 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/analytics-export.ts — new, shared export utilities
- src/app/api/v1/analytics/views/route.ts — new
- src/app/api/v1/analytics/visitors/route.ts — new
- src/app/api/v1/analytics/feedback/route.ts — new
- src/app/api/v1/analytics/searches/route.ts — new
- src/app/api/v1/analytics/assistant-conversations/route.ts — new
- src/app/api/v1/analytics/assistant-caller-stats/route.ts — new
- src/app/api/v1/analytics/feedback-by-page/route.ts — new
- tests/analytics-export.test.ts — new, 13 unit tests
- tests/e2e/analytics-export.spec.ts — new, 21 E2E tests

### Decisions
- Used /api/v1/analytics/* path to match existing v1 API pattern
- Admin-only auth: assistant keys get 401 (not 403) since they shouldn't know these endpoints exist
- Response envelope uses { data, pagination } for consistency and forward-compatibility
- Views endpoint aggregates by date; visitors adds top page/referrer per day via mode() aggregate
- Searches aggregates by query string with count; feedback-by-page uses filter(where) for helpful/not_helpful counts
- Assistant conversations returns full message arrays; caller-stats aggregates using jsonb_array_length
- Date boundaries: dateFrom → T00:00:00Z, dateTo → T23:59:59.999Z (inclusive range)

## Iteration 17 — api-004: REST API Agent Endpoints
Date: 2026-04-09

### What was built
- Agent job REST API with admin key authentication:
  - `POST /api/v1/agent/create-job` — creates an agent job for a project, starts simulated background processing (pending → running → succeeded)
  - `GET /api/v1/agent/get-job/{jobId}` — returns job status, PR link, and messages; validates org ownership
  - `POST /api/v1/agent/send-message/{jobId}` — appends a follow-up user message to a job's messages array; 409 for completed/failed jobs
- Utility library `src/lib/api-v1-agents.ts` with validation (UUID, create-job input, send-message input) and response formatting
- Added `messages` JSON column to `agent_jobs` schema — stores array of `{role, content, timestamp}` objects
- Schema pushed to RDS Postgres via `make db-push`

### Test results
- 22 new unit tests (api-v1-agents.test.ts) — all passing
- 6 new E2E tests (agent-api.spec.ts) — written, QA will verify
- 551 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/db/schema.ts — added `messages` column to agentJobs table
- src/lib/api-v1-agents.ts — new, validation + formatting utilities
- src/app/api/v1/agent/create-job/route.ts — new, POST create-job endpoint
- src/app/api/v1/agent/get-job/[jobId]/route.ts — new, GET get-job endpoint
- src/app/api/v1/agent/send-message/[jobId]/route.ts — new, POST send-message endpoint
- tests/api-v1-agents.test.ts — new, 22 unit tests
- tests/e2e/agent-api.spec.ts — new, 6 E2E tests

### Decisions
- Initial user prompt is stored as first message in the messages array on job creation
- Only pending/running jobs accept new messages (409 Conflict for succeeded/failed)
- Background processing is simulated with setTimeout (pending→running at 500ms, running→succeeded at 5s)
- PR URL is auto-generated during simulation for demo purposes

## Iteration 18 — feature-007c: Analytics Assistant Tab
Date: 2026-04-09

### What was built
- Analytics assistant utility library (`src/lib/analytics-assistant.ts`) with types (DailyConversationCount, AssistantCategory, ChatHistoryEntry, AssistantAnalyticsData), sub-tab config, message extraction, truncation, conversation categorization (9 categories), relative date formatting, daily count gap-filling, CSV export (escape, categories CSV, chat history CSV, browser download)
- API route `GET /api/analytics/assistant?projectId=&from=&to=` — aggregates assistant_conversations into daily counts, auto-categorizes conversations by first user message keywords, returns chat history with message previews, total conversations/messages counts, with auth + org membership verification
- Assistant analytics page (`/analytics/assistant`) — full implementation with:
  - Categories and Chat history sub-tab toggle buttons
  - Export to CSV button (downloads categories or chat history CSV based on active sub-tab)
  - Categories table: category name + conversation count, sorted descending
  - Chat history table: first message preview (truncated 80 chars), message count, relative date
  - Empty state: "No assistant activity / When users ask questions, results will show up here" with MessageSquare icon
  - Agent mode empty state variant
  - Loading skeleton with animated pulse placeholders
  - Summary line showing total conversations and messages

### Test results
- 33 new unit tests (analytics-assistant.test.ts) — all passing
- 6 new E2E tests (analytics-assistant.spec.ts) — written, QA will verify
- 584 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/analytics-assistant.ts — new, assistant analytics types, helpers, CSV export
- src/app/api/analytics/assistant/route.ts — new, assistant analytics API endpoint
- src/app/analytics/assistant/page.tsx — new, assistant analytics page with sub-tabs and CSV export
- tests/analytics-assistant.test.ts — new, 33 unit tests
- tests/e2e/analytics-assistant.spec.ts — new, 6 E2E tests
- prd.json — feature-007c build_pass set to true
- qa-hints.json — added feature-007c QA hints

### Decisions
- Conversation categorization uses keyword regex on first user message — 9 categories covering setup, API, troubleshooting, deployment, auth, general questions, guides, UI, and other
- Categories computed server-side from up to 200 conversations — sufficient for analytics without heavy aggregation
- Chat history limited to 50 most recent conversations — keeps response size manageable
- CSV export is client-side (blob download) — no server endpoint needed, works offline
- Sub-tabs are client-side state (not URL params) — keeps URL clean, Categories is default
- Export CSV button disabled when no data — prevents empty file downloads

## Iteration 19 — feature-007d: Analytics Searches Tab
Date: 2026-04-09

### What was built
- Analytics searches library (`src/lib/analytics-searches.ts`) with types (DailySearchCount, SearchQuery, SearchesData) and helpers (formatSearchCount, sumSearchCounts, truncateQuery)
- Dashboard API endpoint (`src/app/api/analytics/searches/route.ts`) — queries analytics_events WHERE type='search', groups by LOWER(data->>'query') for case-insensitive deduplication, returns dailyCounts, topSearches (top 20), totalSearches
- Searches page (`src/app/analytics/searches/page.tsx`) with:
  - Search volume chart (ComposedChart with bars + line, indigo color scheme)
  - Top searches table (query + count columns)
  - Empty state: "No search activity / When users search your docs, results will show up here"
  - Agent mode empty state directing to MCP Searches tab
  - Loading skeleton matching other analytics tabs
- Auth + org membership verification on API route (same pattern as views/visitors)

### Test results
- 16 new unit tests (analytics-searches.test.ts) — all passing
- 5 new E2E tests (analytics-searches.spec.ts) — written, QA will verify
- 600 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/analytics-searches.ts — new, types + helpers
- src/app/api/analytics/searches/route.ts — new, dashboard API endpoint
- src/app/analytics/searches/page.tsx — new, Searches tab page
- tests/analytics-searches.test.ts — new, 16 unit tests
- tests/e2e/analytics-searches.spec.ts — new, 5 E2E tests
- prd.json — feature-007d build_pass set to true
- qa-hints.json — appended QA hints

### Decisions
- Used indigo (#6366f1) color for search charts to differentiate from green (views/visitors)
- Case-insensitive query grouping via LOWER() in SQL for better deduplication
- Empty state text matches PRD spec exactly
- Followed Views tab pattern closely for consistency

## Iteration 20 — feature-007e: Analytics Feedback Tab
Date: 2026-04-09

### What was built
- Analytics feedback utility library (`src/lib/analytics-feedback.ts`) with types (FeedbackEntry, RatingsByPage, FeedbackStatus, FeedbackSubTab), sub-tab configuration (Ratings by page, Detailed feedback, Code snippets), status definitions with labels/colors (Pending/In Progress/Resolved/Dismissed), filter helpers (filterByStatus, filterBySubTab), CSV export (feedbackToCsv, ratingsToCsv, downloadCsv), formatting (formatFeedbackDate, truncateFeedback, parseFeedbackStatus)
- API route `GET /api/analytics/feedback?projectId=&from=&to=` — returns individual feedback entries with status/type/abusive flag, aggregated ratingsByPage (helpful/notHelpful/total per page), and totalCount. Auth + org membership verification. Feedback type stored in data.feedbackType JSONB field
- Feedback tab page (`/analytics/feedback`) — full implementation with:
  - 3 sub-tabs: Ratings by page (table with helpful/not_helpful/total columns), Detailed feedback (type=contextual entries table), Code snippets (type=code_snippet entries table)
  - Filters button → dialog with 4 Status checkboxes (Pending/In Progress/Resolved/Dismissed) + Show abusive toggle
  - Active filters indicator (emerald highlight + count badge on Filters button)
  - Feedback entries table: checkbox, feedback text, status badge (colored), page path, date
  - Select all checkbox in header + individual row checkboxes for bulk select
  - Bulk actions bar showing count of selected items
  - Ratings table: page path, helpful (emerald), not helpful (red), total columns
  - CSV export button for both ratings and feedback entries
  - Loading skeleton state with animated pulse placeholders
  - Agent mode empty state: "No feedback activity"
  - Empty state: "No feedback yet" when no data

### Test results
- 22 new unit tests (analytics-feedback.test.ts) — all passing
- 5 new E2E tests (analytics-feedback.spec.ts) — written, QA will verify
- 622 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/analytics-feedback.ts — new, feedback types, sub-tabs, statuses, filters, CSV export
- src/app/api/analytics/feedback/route.ts — new, internal feedback analytics API endpoint
- src/app/analytics/feedback/page.tsx — new, feedback tab with sub-tabs, filters, bulk select
- tests/analytics-feedback.test.ts — new, 22 unit tests
- tests/e2e/analytics-feedback.spec.ts — new, 5 E2E tests
- prd.json — feature-007e build_pass set to true
- qa-hints.json — added feature-007e QA hints

### Decisions
- Sub-tabs switch content client-side (no URL param for type) — simpler than separate routes, matches Assistant tab pattern
- Status stored in JSONB data.status field on analytics_events — avoids schema migration for a new column
- Feedback type stored in data.feedbackType — "contextual" for detailed feedback, "code_snippet" for code snippets, "rating" default
- Ratings by page uses SQL FILTER (WHERE ...) aggregation for helpful/not_helpful counts — efficient single query
- Filter dialog is positioned absolute from Filters button — no portal needed
- Bulk select clears when switching sub-tabs — prevents stale selections across different entry sets
- CSV export adapts to active sub-tab: ratings tab exports page aggregates, other tabs export filtered entries

## Iteration 21 — feature-009: Team Management
Date: 2026-04-09

### What was built
- Members utility library (`src/lib/members.ts`) with invite validation (email format, role check, trim/lowercase), role update validation, admin-only permission check (`canManageRole`), and response formatting
- Members API routes:
  - `GET /api/members` — lists all org members with user details (name, email, image) via join on Better Auth user table
  - `POST /api/members` — invite user by email (must have existing account), validates email/role, checks for duplicates (409), admin-only
  - `PATCH /api/members/[membershipId]/role` — update member role, admin-only, cannot change own role
  - `DELETE /api/members/[membershipId]` — remove member, admin-only, cannot remove self
- Members settings page (`/settings/organization/members`) with:
  - Member table: avatar (image or initials fallback), name, email, role badge (colored: emerald=admin, blue=editor, gray=viewer), joined date, actions
  - Invite modal: email input + role select dropdown, validates on submit, shows success/error messages
  - Inline role editing: click "Edit role" → select dropdown with Save/Cancel
  - Remove member: confirmation modal with member name, red delete button
  - Member count display, breadcrumb navigation (Settings / Members)

### Test results
- 17 new unit tests (members.test.ts) — all passing
- 5 new E2E tests (members.spec.ts) — written, QA will verify
- 639 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/members.ts — new, validation + permission + formatting utilities
- src/app/api/members/route.ts — new, GET list + POST invite
- src/app/api/members/[membershipId]/route.ts — new, DELETE remove member
- src/app/api/members/[membershipId]/role/route.ts — new, PATCH update role
- src/app/settings/organization/members/page.tsx — new, members management UI
- tests/members.test.ts — new, 17 unit tests
- tests/e2e/members.spec.ts — new, 5 E2E tests

### Decisions
- Members added by email lookup (must have existing account) — no email invite delivery system needed
- Role changes admin-only — editors and viewers cannot modify team
- Cannot change own role or remove self — prevents accidental lockout
- User table joined via Better Auth's `user` table for name/email/image
- Avatar shows profile image if available, initials fallback otherwise
- Role badges color-coded: emerald (admin), blue (editor), gray (viewer)

## Iteration 22 — feature-010: Agent Management Page
Date: 2026-04-09

### What was built
- Agent dashboard utility library (`src/lib/agent-dashboard.ts`) with status helpers (label, color), prompt truncation, timeAgo formatter, prompt validation, PR number extraction
- Internal session-authenticated API routes for dashboard agent management:
  - `GET /api/agent/jobs` — list agent jobs for user's active project (max 50, newest first)
  - `POST /api/agent/jobs` — create a new agent job with prompt validation
  - `GET /api/agent/jobs/[jobId]` — get single job with messages and org-scoped access check
  - `POST /api/agent/jobs/[jobId]/messages` — send follow-up message with agent reply simulation
- Agent page (`/products/agent`) with full Mintlify-matching UI:
  - Page header with "Agent" title and refresh button
  - Prompt input form at top to create new jobs
  - Job list view: prompt preview (truncated), status badge (color-coded with pulse on running), timestamp, PR link
  - Job detail view: back button, full prompt, status, PR link with external link icon, message thread (user messages right-aligned green, agent messages left-aligned gray), follow-up message input
  - Empty state with bot icon and "Enable the Agent" messaging matching screenshot
  - Auto-refresh every 5s when active jobs exist (pending/running)
  - Background simulation: jobs transition pending → running (0.5s) → succeeded with PR URL (5s)

### Test results
- 15 new unit tests (agent-dashboard.test.ts) — all passing
- 5 new E2E tests (agent-page.spec.ts) — written, QA will verify
- 654 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/agent-dashboard.ts — new, dashboard utility functions
- src/app/api/agent/jobs/route.ts — new, GET + POST session-auth API routes
- src/app/api/agent/jobs/[jobId]/route.ts — new, GET single job detail
- src/app/api/agent/jobs/[jobId]/messages/route.ts — new, POST follow-up message
- src/app/products/agent/page.tsx — new, server component fetching jobs
- src/app/products/agent/agent-page-client.tsx — new, full agent page UI with list + detail views
- tests/agent-dashboard.test.ts — new, 15 unit tests
- tests/e2e/agent-page.spec.ts — new, 5 E2E tests

### Decisions
- Internal API routes use session auth (getServerSession) not API key auth — separate from v1 REST API
- Job simulation reuses same pattern from v1 create-job route (setTimeout-based transitions)
- Follow-up messages get an immediate simulated agent reply — in production would be async
- Access control: verify user is member of org that owns the project
- Auto-refresh uses setInterval with cleanup, only when active jobs exist to avoid unnecessary polling

## Iteration 23 — feature-015: Interactive API Playground
Date: 2026-04-09

### What was built
- OpenAPI 3.x spec parser (`src/lib/openapi-parser.ts`) — extracts endpoints, parameters (path/query), request bodies, auth schemes, server base URLs from OpenAPI spec objects
- Example JSON generator from JSON Schema — auto-populates request body textarea with typed example values (string, integer, email format, etc.)
- API playground HTML renderer — generates interactive HTML for each endpoint: method badge (color-coded GET/POST/PUT/DELETE), URL bar, parameter inputs, request body editor, auth header input with bearer pre-fill, Send button, response area with body/headers tabs
- API playground client component (`src/components/docs/api-playground.tsx`) — wires Send button click handlers, collects parameters from inputs, builds URL with path/query params, sends request via proxy, displays response with status code, timing, formatted body/headers, tab switching
- API proxy route (`POST /api/docs/proxy`) — forwards playground requests to external APIs to avoid CORS; includes SSRF protection (blocks localhost, 127.0.0.1, private IPs, .internal/.local domains); strips host/origin/referer headers
- Docs page integration — API reference pages (path starts with `api-reference`) auto-render playground when project settings contain `openApiSpec`; endpoint matching via page frontmatter `api` field (e.g., `api: "GET /users/{id}"`)
- MDX `<ApiPlayground>` component support — renders marker div for custom playground placement in MDX content
- Updated `validateUpdateProjectRequest` to accept `settings` JSONB field for storing OpenAPI spec in project settings
- Comprehensive CSS styles matching Mintlify dark theme — method badges, parameter inputs, body textarea, send button (green), response area with status code coloring (2xx green, 4xx amber, 5xx red), response tabs, light theme overrides

### Test results
- 20 new unit tests (api-playground.test.ts) — all passing
- 5 new E2E tests (api-playground.spec.ts) — written, QA will verify
- 674 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/openapi-parser.ts — new, OpenAPI spec parser + HTML renderer
- src/components/docs/api-playground.tsx — new, client-side playground interactivity
- src/app/api/docs/proxy/route.ts — new, CORS proxy with SSRF protection
- src/app/docs/[subdomain]/[...slug]/page.tsx — updated, API playground integration
- src/lib/mdx-renderer.ts — updated, added ApiPlayground component
- src/lib/projects.ts — updated, settings field in update validation
- src/app/globals.css — updated, API playground styles
- tests/api-playground.test.ts — new, 20 unit tests
- tests/e2e/api-playground.spec.ts — new, 5 E2E tests

### Decisions
- OpenAPI spec stored in project settings JSONB field (`settings.openApiSpec`) — flexible, no schema migration needed
- CORS proxy approach instead of client-side fetch — avoids browser CORS restrictions for external APIs
- SSRF protection blocks all private/internal addresses — security best practice for server-side proxy
- Endpoint matching via frontmatter `api` field — allows per-page endpoint specification matching Mintlify's pattern
- Custom MDX parser (no external OpenAPI library) — keeps bundle small, handles common OpenAPI 3.x patterns
- Response area hidden by default, shown only after Send — clean initial state
- Light theme overrides for all playground elements — consistent with existing docs theme system

## Iteration 24 — feature-015a: Docs Site API Reference Layout
Date: 2026-04-09

### What was built
- API reference utility library (`src/lib/api-reference.ts`) with:
  - Method badge mapping (GET=green, POST=blue, PUT/PATCH=amber, DELETE=red, HEAD=purple, HOOK=purple)
  - Sidebar endpoint metadata parser — extracts HTTP method from frontmatter `api` field
  - cURL code generation from OpenAPI endpoints with auth headers and request body
  - Multi-language code examples generator (cURL, Python, JavaScript)
  - Response schema field extractor with type formatting (handles format, enum, array items)
  - Response tab extractor with status code sorting
  - Full API reference page HTML renderer: method+URL header, code block with language selector, response tabs, auth section, parameters section, response schema
- Updated `DocsNavItem` type to include optional `apiMethod` field
- Updated `buildDocsNav` to extract API method from page frontmatter for sidebar badge display
- Updated sidebar component (`docs-sidebar.tsx`) to show colored method badges (GET/POST/DEL/HOOK) instead of file icons for API reference endpoints
- Created API reference layout client component (`api-reference-layout.tsx`) with interactive event wiring:
  - Language tab switching for code examples
  - Code copy button with clipboard API
  - Ask AI button dispatching custom event
  - Response status tab switching (200/400)
  - Try it button scrolling to API playground
- Updated docs page to render API reference layout for matched OpenAPI endpoints
- Added comprehensive CSS styles: sidebar badges (compact inline), method+URL header, code section with lang tabs, response status tabs, auth section, parameters table with type/required badges, all with light theme overrides

### Test results
- 38 new unit tests (api-reference.test.ts) — all passing
- 5 new E2E tests (api-reference-layout.spec.ts) — written, QA will verify
- 712 total unit tests — all passing
- make check (typecheck + lint) — passing

### Files changed
- src/lib/api-reference.ts — new, API reference utilities (badges, cURL, schema, renderer)
- src/lib/mdx-renderer.ts — updated, DocsNavItem type + buildDocsNav frontmatter support
- src/components/docs/docs-sidebar.tsx — updated, method badge rendering for API reference items
- src/components/docs/api-reference-layout.tsx — new, client-side interactivity for API reference page
- src/app/docs/[subdomain]/[...slug]/page.tsx — updated, API reference layout integration
- src/app/globals.css — updated, sidebar badge + API reference page styles with light theme
- tests/api-reference.test.ts — new, 38 unit tests
- tests/e2e/api-reference-layout.spec.ts — new, 5 E2E tests

### Decisions
- Method badges in sidebar use compact inline styling (0.5625rem font, 3px radius) to fit nav items
- DELETE shows as "DEL" and webhooks as "HOOK" matching Mintlify's abbreviated labels
- API method extracted from frontmatter `api` field format "METHOD /path" — same pattern as existing playground matching
- Code examples generate 3 languages (cURL, Python, JavaScript) — cURL shown by default
- Response tabs show placeholder JSON examples based on status code range (2xx = success, 4xx = error)
- Try it button scrolls to the existing API playground section rather than duplicating functionality
- Used biome-ignore for useEffect dependency (html triggers re-wiring) following existing ApiPlayground pattern

## Iteration 25 — feature-018: Agent Settings Page
Date: 2026-04-09

### What was built
- Agent settings utility library (`src/lib/agent-settings.ts`) with types (ConnectedRepo, AgentSettingsData), plan gating check, repo name formatter, connected repos validator, settings update request validator
- New `agent_settings` DB table with fields: orgId (unique), agentEnabled, slackConnected, slackWorkspace, githubAppInstalled, connectedRepos (JSONB array)
- API route `GET/PUT /api/agent/settings` — session-authenticated, resolves org + plan, upserts settings, enforces plan gating (agent requires Pro/Enterprise), role-based access (viewers cannot update)
- Agent settings client component (`src/app/products/agent/agent-settings-client.tsx`) with:
  - Enable Agent section: toggle switch (plan-gated) or "Upgrade plan" button for free plan, "Learn more" link
  - Slack Connection section: connection status badge (Connected/Not connected), workspace name display, "Connect Slack workspace" button
  - GitHub App section: installation status, "Configure GitHub app" link, connected repos list with org/repo name + branch + permissions badge
  - Linear Agent promo sidebar card: "New" badge, description, "Configure Linear" link, dismissible via X button
- Updated agent page server component to support `?tab=settings` (default) and `?tab=jobs` views, fetching org plan and agent settings from DB
- Custom SVG icons for GitHub and Slack (lucide-react lacks these)

### Test results
- 26 new unit tests (agent-settings.test.ts) — all passing
- 6 new E2E tests (agent-settings.spec.ts) — written, QA will verify
- 738 total unit tests — all passing
- make check (typecheck + lint) — passing
- Schema pushed to RDS Postgres via make db-push — success

### Files changed
- src/lib/db/schema.ts — added agentSettings table
- src/lib/agent-settings.ts — new, types + validation + helpers
- src/app/api/agent/settings/route.ts — new, GET/PUT API endpoint
- src/app/products/agent/agent-settings-client.tsx — new, settings UI client component
- src/app/products/agent/page.tsx — updated, tab routing (settings/jobs), org plan + settings fetching
- tests/agent-settings.test.ts — new, 26 unit tests
- tests/e2e/agent-settings.spec.ts — new, 6 E2E tests
- prd.json — feature-018 build_pass set to true
- qa-hints.json — added feature-018 QA hints

### Decisions
- Used a dedicated `agent_settings` table (not JSONB in orgs.settings) for type safety and queryability
- Settings page is default view; jobs accessible via `?tab=jobs` query param
- Linear promo card state is local (useState) — dismissal doesn't persist across page reloads
- Slack and GitHub integration buttons are UI shells — actual OAuth flows are out of scope for this feature
- Plan gating enforced both client-side (UI) and server-side (API returns 403 for free plan)
- Used custom SVG icons for GitHub and Slack since lucide-react doesn't include them
