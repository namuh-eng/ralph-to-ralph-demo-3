import { SettingsPlaceholder } from "@/components/settings/settings-placeholder";

export default function GitSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Git Settings"
      breadcrumb="Settings / Deployment / Git Settings"
    />
  );
}
