import { SettingsPlaceholder } from "@/components/settings/settings-placeholder";

export default function GithubAppSettingsPage() {
  return (
    <SettingsPlaceholder
      title="GitHub App"
      breadcrumb="Settings / Deployment / GitHub App"
    />
  );
}
