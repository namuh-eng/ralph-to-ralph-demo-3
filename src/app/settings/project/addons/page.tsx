import { SettingsPlaceholder } from "@/components/settings/settings-placeholder";

export default function AddonsSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Add-ons"
      breadcrumb="Settings / Project Settings / Add-ons"
    />
  );
}
