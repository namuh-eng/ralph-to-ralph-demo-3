import { SettingsPlaceholder } from "@/components/settings/settings-placeholder";

export default function DomainSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Domain Setup"
      breadcrumb="Settings / Project Settings / Domain Setup"
    />
  );
}
