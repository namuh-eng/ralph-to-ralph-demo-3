import { SettingsPlaceholder } from "@/components/settings/settings-placeholder";

export default function AuthenticationSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Authentication"
      breadcrumb="Settings / Project Settings / Authentication"
    />
  );
}
