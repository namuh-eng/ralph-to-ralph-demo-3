import { SettingsPlaceholder } from "@/components/settings/settings-placeholder";

export default function ProfileSettingsPage() {
  return (
    <SettingsPlaceholder
      title="My Profile"
      breadcrumb="Settings / Workspace / My Profile"
    />
  );
}
